{
  "name": "qadam-parent-mvp",
  "version": "0.1.0",
  "private": true,
  "description": "Bilingual adaptive home-practice platform with a growing library of parent-guided exercises.",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "check": "node scripts/check.mjs",
    "lint": "node scripts/check.mjs",
    "test": "node --test tests/*.test.js",
    "verify": "npm run build && npm run check && npm run test",
    "serve": "node scripts/serve.mjs",
    "exercise:inventory": "node scripts/check-exercise-inventory.mjs"
  },
  "dependencies": {
    "@zxing/browser": "0.2.1",
    "pdfjs-dist": "6.1.200"
  }
}
