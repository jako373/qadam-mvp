# Current MVP uses localStorage and does not need environment variables.
# When Supabase is connected later, use browser-safe public keys only.

NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=

# Never expose a service_role key in browser code or any NEXT_PUBLIC_* variable.
SUPABASE_SERVICE_ROLE_KEY=
