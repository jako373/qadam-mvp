# The current static MVP does not read environment variables.
# Keep Supabase secrets out of all browser code and committed files.
# Add only a browser-safe URL and publishable key after Auth integration is implemented.
